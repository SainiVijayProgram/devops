name: 'GitHub Workflow Demo'

on:
  push:
    branches:
    - "dev"
    
  pull_request:
  
  workflow_dispatch:
    environment:
      description: 'Environment to run tests against'
      type: environment
      required: true 


jobs:
  sayHello:
    runs-on: ubuntu-latest
    
    steps: 
    - name: runscript
      run:  echo "Hello There"
   
  sayBye:
    runs-on: windows-latest
    
    steps: 
    - run:  echo "Good Bye!!! See you around"
